services:

  backend:

    image: websocket-backend

    container_name: websocket-backend

    ports:
      - "8000:8000"


  redis:

    image: redis:latest

    container_name: websocket-redis

    ports:
      - "6379:6379"